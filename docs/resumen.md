# Resumen del proyecto

> Se actualiza al final de cada sesión. Léelo primero para saber dónde retomar.

**Última actualización**: 2026-08-13 (RFC-0029: auditoría + implementación del sistema de mensajes al usuario — códigos de error estables en el API, capa única de copy en el frontend, mensajes de validación Zod en español, toasts de éxito. Detalle en "Qué se hizo en esta sesión" abajo. En la misma sesión se confirmó cerrado el golden path de RFC-0023.3, ver abajo).

**Golden path de RFC-0023.3** (validado en la sesión previa, 2026-08-12): rename sin diálogo, cambio de precio con diálogo + "Cambios pendientes", "Desactualizado" en escenarios compuestos, archivado y composición se verificaron manualmente y funcionan como especifica RFC-0023.3. **No hubo commit porque no requirió cambiar ningún archivo** — era validación, no corrección. Era la validación funcional más antigua sin confirmar del proyecto; ya no está pendiente.

**RFC-0028** (2026-08-11): auditoría + implementación responsive/UX/UI de Escenarios, Categorías, Productos e Ingresos, más un ajuste posterior de UX en desktop (animación de diálogos y densidad de grids). Sin features nuevas, sin cambios de backend/Prisma. Detalle completo ya integrado en "Estado" abajo.

**RFC-0026** (2026-08-07, PR #23): cierre final de RFC-0025 en Escenarios (badge de categoría, mini-cards para Ingresos y Escenarios incluidos) + unificación visual de Categorías/Productos/Ingresos sobre ese mismo lenguaje de tarjetas (`ExpenseItemCard`/`CategoryCard`/`IncomeCard`) + "Usado en estos escenarios" en Ingresos + logo real de Vectra en el navbar. Detalle completo ya integrado en "Estado" abajo.

**RFC-0027** (2026-08-07, mismo día): retiro completo del ledger (Cuentas, Transacciones, Presupuestos, Transacciones recurrentes, Reports, Dashboard) de la experiencia de producto y del backend expuesto — [ADR-0007](decisions/0007-retire-ledger-surface.md). Detalle completo ya integrado en "Estado" abajo.

## Estado

**Backend**: expone hoy solo el dominio de escenarios — auth (JWT + refresh), categories, expense-items, incomes (RFC-0021), scenarios (RFC-0022), sus endpoints `summary` (RFC-0023), eliminación de categorías con reasignación (RFC-0023.2) y el sistema de sincronización en el momento de editar (RFC-0023.3, ver abajo). Todo en `apps/api`. **El ledger (accounts, transactions, budgets, recurring-transactions, dashboard, reports) se retiró por completo en RFC-0027** ([ADR-0007](decisions/0007-retire-ledger-surface.md)): sin rutas, servicios, schemas ni tests, junto con la lib exclusiva a ellos (`resource-guards`, `chart-color`, `finance-math`, `date-range`, `report-buckets`, `recurrence`). Los modelos Prisma (`Account`, `Transaction`, `Budget`, `RecurringTransaction`) siguen en el schema sin migración ni endpoint que los exponga — `Category.archiveCategory`/`deleteCategory` sigue dependiendo de esas relaciones para su propia cascada de archivado y conteo. Nuevos registros usan `COP` como `defaultCurrency` por defecto (el usuario de prueba sembrado sigue en USD); el registro ya no crea una cuenta "Efectivo" por defecto (no tiene sentido sembrar un modelo sin endpoint que lo exponga). CORS permite explícitamente `GET/HEAD/POST/PATCH/DELETE`.

**Escenarios (modelo)**: `Scenario` (estados `ACTIVE`/`INACTIVE`/`ARCHIVED`, default `INACTIVE`) con `ScenarioItem`/`ScenarioIncome` como snapshots explícitos de `ExpenseItem`/`Income` — el escenario nunca lee el recurso original en vivo, así que un cambio de precio nunca modifica una simulación en silencio (ADR-0005). `ScenarioComposition` permite escenario-en-escenario con detección de ciclos (BFS en service layer). Los `ScenarioItem` **sí** se heredan a través de la composición (agregado real); los `ScenarioIncome` **no** — un ingreso pertenece únicamente al escenario donde se vinculó.

**`ScenarioItem` snapshotea `categoryName` y `frequencyOverride`**:

- `categoryName: String` — el nombre de la categoría al momento de agregar el producto. Nunca se lee la categoría en vivo (ADR-0005); un rename en la categoría fuente se sincroniza **solo, sin diálogo ni decisión del usuario** vía `syncCategoryNameInScenarios`, porque es 100% visual y nunca mueve un total.
- `frequencyOverride: Boolean` — `true` cuando la frecuencia de ese `ScenarioItem` fue elegida a propósito para ese escenario (ej. simular un producto facturado anualmente ahí mientras el producto real sigue siendo mensual), en vez de copiada del `ExpenseItem` vivo. Se fija al agregar un producto desde "Desde categorías" con una frecuencia distinta a la original (`POST /scenarios/:id/items` acepta `frequency` opcional). Cuando está en `true`, `diffItemKinds` **excluye** la frecuencia de la detección de drift — nunca aparece como "Desactualizado" por esto, y ni `syncExpenseItemScenarios` ni `syncScenario` la pisan de vuelta a la del producto real al aplicar un cambio de precio.

**Las categorías no tienen apariencia almacenada**. No hay columna `icon` ni `categoryIcon`, ni vocabulario de iconos, ni picker. El color de una categoría se **deriva de su nombre** en el cliente (`apps/web/src/features/categories/category-color.ts`): hash djb2 sobre el nombre normalizado (trim + lowercase) contra una paleta cerrada de 8 tonos. Renombrar una categoría cambia su color, y eso es aceptable a propósito: el color sirve para distinguir de un vistazo, no es una identidad. La paleta excluye ámbar y rojo porque ámbar ya significa "Desactualizado" en una tarjeta de producto y rojo significa destructivo en el resto de la app. Tres variantes por color: `soft` (badges/chips), `solid` (chip seleccionado), y `wash` (agregada en RFC-0026 — fondo/borde de baja opacidad, sin tocar el color de texto, para teñir una tarjeta completa como identidad visual sin competir con su contenido; la usa `CategoryCard`). Este sistema de color ya no es exclusivo de Escenarios: `ExpenseItemCard` y `CategoryCard` lo reutilizan directamente (copiando la misma receta de clases, sin importar nada de `features/scenarios`).

**Sincronización en el momento de editar (RFC-0023.3)**: reemplaza por completo el sistema anterior de "revisar cambios acumulados" (banner permanente + lista con checkboxes). El flujo es preventivo, no acumulativo:

1. El `PATCH`/`archive`/`unarchive` de un producto o ingreso **siempre guarda**, sin bloquearse ni pedir confirmación previa.
2. En la misma operación, los cambios **visuales** (nombre del producto, nombre de la categoría, nombre del ingreso) se sincronizan solos en todos los snapshots no archivados — nunca generan pregunta, porque no mueven ningún total.
3. Después de guardar, la respuesta incluye `affectedScenarios`: los escenarios no archivados cuyo costo **sí** se movería (cambios financieros: precio, frecuencia sin `frequencyOverride`, archivado). Para productos incluye los escenarios padre por composición (`collectAncestorScenarioIds`, BFS ascendente); para ingresos no, porque la cobertura de un padre nunca lee los ingresos de un hijo.
4. Si la lista viene vacía, el flujo termina ahí. Si no, el frontend abre `ScenarioImpactDialog` — el recurso ya está guardado, el diálogo solo decide la sincronización.
5. "Aplicar ahora" → `POST /expense-items/:id/sync-scenarios` (o `/incomes/:id/sync-scenarios`). "No ahora" → ninguna llamada extra; esos escenarios quedan con `hasUpdates: true`.

**El diálogo dice qué va a cambiar, no solo cuántos escenarios**: la misma respuesta trae `changes`, un resumen agregado **por campo** derivado de los `diff*Kinds` que ya se calculaban (`summarizeItemImpact`/`summarizeIncomeImpact`). Sigue siendo todo-o-nada: sin checkboxes ni selección por cambio. Los snapshots afectados pueden discrepar sobre el valor viejo (el escenario A se sincronizó a 80.000 y el B sigue en 60.000), así que `from` es `number | null` y en ese caso se muestra solo el destino en vez de inventar un origen.

**El aviso "Cambios pendientes" del escenario también es descriptivo**, con el mismo describer que el diálogo — no dos implementaciones. Cada `ScenarioImpactChange` lleva `name` (del producto/ingreso) y `source: "expenseItem" | "income"`. Un único mapper en el backend, `toScenarioImpactChange`, convierte los `ScenarioChange` que ya calculaba `detectScenarioChanges` (infraestructura de RFC-0023.1, conservada) al mismo formato `ScenarioImpactChange` — dos rutas de cálculo, un solo formato de salida. `GET /scenarios/:id/summary` lo expone como `pendingChanges: ScenarioImpactChange[]`, derivado del mismo array que ya alimentaba `hasUpdates` (`hasUpdates: pendingChanges.length > 0`) — cero queries extra. En el frontend, `describeScenarioChange` (`apps/web/src/features/scenarios/describe-scenario-change.ts`) es la única función que convierte un cambio a texto en español; la usan tanto `ScenarioImpactDialog` como `ScenarioSummaryCards`.

**Nada derivado se almacena**: `hasUpdates` se calcula al vuelo (solo con cambios `kind: "financial"`, y devuelve `false` sin más para escenarios `ARCHIVED`). Se descartó explícitamente guardar un flag `hasPendingChanges` por ser el mismo patrón desincronizable que RFC-0023.1 vino a corregir.

**Escenarios archivados**: congelados a nivel de dominio. No entran en `affectedScenarios`, no se sincronizan, y su `summary` reporta `hasUpdates: false`. Al desarchivar se vuelve a comprobar desde cero (el cálculo es derivado, así que esto sale gratis). En el frontend no desaparecen del sidebar al archivarlos (sección "Archivados" propia).

**Botón "Actualizar" a nivel escenario**: `POST /scenarios/:id/sync` aplica de una vez todos los cambios financieros pendientes alcanzables (items propios + los de escenarios compuestos, ingresos propios), sin selección por ítem. Respeta `frequencyOverride`.

**Infraestructura `ScenarioChange` conservada**: la unión discriminada de 9 variantes (`ITEM_RENAMED`, `ITEM_CATEGORY_RENAMED`, `ITEM_PRICE_CHANGED`, `ITEM_FREQUENCY_CHANGED`, `ITEM_ARCHIVED`, `INCOME_RENAMED`, `INCOME_AMOUNT_CHANGED`, `INCOME_FREQUENCY_CHANGED`, `INCOME_ARCHIVED`) con su `kind: "visual" | "financial"`, los detectores (`CHANGE_DETECTORS`, `diffItemKinds`/`diffIncomeKinds`) y los endpoints `GET/POST /scenarios/:id/changes(/apply)` siguen existiendo. **Ninguna pantalla los consume hoy** — se mantienen como capacidad reutilizable. Los `diff*Kinds` siguen siendo la única fuente de verdad, compartida por el flag `outdated` por ítem, `hasUpdates` y todas las funciones de sync.

**Eliminado en sesiones previas**: el modelo `ScenarioCategoryWatch` (tabla, endpoints `category-watches`, relaciones), el tipo de cambio `NEW_ITEM_AVAILABLE`, y el sistema de iconos de categoría (columna `icon`/`categoryIcon`, vocabulario cerrado, picker). "Categoría completa" pasó a ser **solo una ayuda de selección**: expande la categoría en `ScenarioItem` individuales en el momento, sin mantener ninguna relación viva con ella. También se eliminó `ScenarioChangesDialog`.

**Endpoints `summary` por entidad (RFC-0023)**: `GET /categories/:id/summary`, `GET /expense-items/:id/summary` (+ en qué escenarios se usa), `GET /incomes/:id/summary` (**+ en qué escenarios se usa, agregado en RFC-0026** — mismo patrón que expense-items: `scenarioIncome.findMany` sobre `incomeId`, sin endpoint nuevo, la relación ya existía e indexada), y `GET /scenarios` enriquecido con `monthly` por fila. Todo el cómputo vive en el backend — `toMonthlyEquivalent`/`toProjection` en `packages/utils`.

**Frontend**: infraestructura (RFC-0017) + Productos/Ingresos (RFC-0021 UI) + Escenarios (RFC-0022 UI) + pantallas-pregunta (RFC-0023) + eliminación de categorías con reasignación (RFC-0023.2) + sincronización en el momento de editar (RFC-0023.3) + asistente de composición (RFC-0025) + unificación visual de Categorías/Productos/Ingresos sobre el lenguaje de tarjetas de Escenarios (RFC-0026, sin RFC de backend propio) + **retiro del ledger** (RFC-0027): páginas, hooks, componentes y rutas exclusivos de Cuentas/Transacciones/Dashboard eliminados; el nav pierde el dropdown "Historial" — el nav primario (Escenarios, Categorías, Productos, Ingresos) es hoy el nav completo + **responsive/UX/UI** (RFC-0028, ver "Qué se hizo" abajo). `StatCard` se reubicó de `features/dashboard/` a `apps/web/src/components/` porque `ProjectionStatCards` (compartido por Categorías/Productos/Ingresos/Escenarios) dependía de él — sin ese movimiento, retirar Dashboard rompía los cuatro módulos vigentes. Vite + React 19 + Tailwind v4, `packages/ui` (shadcn/ui + `DataTable` — **sin consumidores desde RFC-0027**, sus tres usos eran Transacciones/Cuentas/Dashboard — /`EmptyState`/`FormDialog`/`Sheet`, nuevo en RFC-0028), `packages/types` (schemas Zod compartidos; módulo `scenario-impact.ts` separado de `scenarios.ts` para evitar un ciclo de imports con `expense-items`/`incomes` — mismo motivo por el que `incomeSummarySchema`/`expenseItemSummarySchema` duplican un enum local de 3 valores en vez de importarlo de `scenarios.ts`) y `packages/utils`.

**Componentes compartidos de página (RFC-0028)**, en `apps/web/src/components/`: `PageContainer` (`max-w-4xl lg:max-w-6xl` — el único sitio que decide cuánto se ensancha una página en desktop), `CardGrid` (dos densidades, `tile` para Productos y `card` para Categorías/Ingresos, con mínimos y gaps que escalan en `sm`/`lg`), `ListPageHeader` (título + descripción + acción primaria) y `Pagination` (contador + anterior/siguiente, no renderiza nada con una sola página). Las tres listas (Categorías/Productos/Ingresos) y las tres páginas de detalle los usan; ninguna repite ya su propio `mx-auto max-w-4xl` ni su propio grid a mano. `DetailPageHeader` (back link + título + badges + menú de acciones) es compartido por las tres páginas de detalle — Escenarios no lo usa porque vive dentro de `ScenariosLayout`, no tiene back link propio.

**`packages/ui` ganó primitivos responsive genéricos (RFC-0028)**, todos reutilizables fuera de este RFC: `Dialog`/`AlertDialog` con `max-h-[calc(100dvh-2rem)]` + scroll interno + gutter lateral (antes no tenían tope de alto ni margen, así que un formulario más alto que el viewport dejaba el botón de guardar inalcanzable); `AlertDialogAction` acepta `variant` (opt-in, no todo `AlertDialog` es una eliminación — "Aplicar ahora" de `ScenarioImpactDialog` no lo es); `SelectContent` con `max-h` contra el alto disponible real de Radix; `Input`/`SelectTrigger` a `text-base md:text-sm` (evita el zoom automático de Safari iOS en campos con fuente <16px); `Card`/`EmptyState` con padding responsive; `FormDialog` con botón Cancelar explícito. Nuevo `focus-ring` como `@utility` en `index.css`, un solo tratamiento de foco para primitivos y para los `<button>` propios de features que antes no tenían ninguno.

**El primitivo `Sheet`** (`packages/ui/src/components/ui/sheet.tsx`, RFC-0028), sobre `@radix-ui/react-dialog` (sin dependencia nueva): drawer anclado al borde izquierdo, hoy solo con `side="left"` porque es lo único que Escenarios necesita. Reemplaza el sidebar fijo de `ScenariosLayout` por debajo de `md` — un `w-64` fijo dejaba 0px de ancho útil al detalle en 320px. El desktop conserva la columna original sin cambios (`hidden md:flex`); ambas comparten el mismo componente de lista (`ScenarioList`) para que no puedan divergir.

**`tailwindcss-animate` nunca estuvo instalado** (descubierto en RFC-0028, no documentado antes): los primitivos usaban clases (`animate-in`, `fade-in-0`, `zoom-in-95`) que ese plugin define, pero el plugin no está en `package.json` — compilaban a nada, así que ningún diálogo, select ni dropdown animó nunca. Resuelto con keyframes propios en `apps/web/src/index.css` (`vectra-dialog-in/out`, `vectra-popover-in/out`, `vectra-overlay-in/out`, `vectra-sheet-in/out-left`) expuestos como `--animate-*` en `@theme inline`, sin agregar la dependencia. Los de Dialog/AlertDialog animan solo `scale` (nunca `translate`): Tailwind v4 compila `-translate-x-1/2 -translate-y-1/2` a la propiedad independiente `translate`, no a `transform` — un keyframe que animara `transform: translate(...)` se sumaba a esa propiedad en vez de reemplazarla, y el diálogo pasaba toda la animación desplazado media anchura/altura de más, con un salto al terminar. Los de popover (Select/Dropdown) animan solo opacidad, porque Radix ya posiciona esos paneles con su propio transform inline. `prefers-reduced-motion` colapsa todas las duraciones a ~0 en `@layer base`.

**`formatMoneyCompact`** (`packages/utils/src/money.ts`, RFC-0028): versión de `formatMoney` para superficies pequeñas (mini-cards, stat cards) — omite decimales cuando el monto es entero y cambia a notación compacta (`US$4,4 M`) desde 1.000.000. `formatMoney` (el formato completo) no cambió; sigue siendo lo que se usa en detalles, formularios y totales. Los cinco componentes de tarjeta (`ExpenseItemCard`, `ScenarioItemCard`, `IncomeCard`, `CategoryCard`, `ScenarioCompositionCard`) y `StatCard`/`ProjectionStatCards` usan la versión compacta con el valor exacto en `title` (tooltip).

**Navegación y pantallas**: `/` redirige a `/scenarios` (Escenarios es la ruta de aterrizaje real, ADR-0006). Nav: Escenarios, Categorías, Productos, Ingresos — **es el nav completo**; el dropdown secundario "Historial" (Dashboard, Cuentas, Transacciones) se retiró en RFC-0027 junto con el resto del ledger. `Budget`/`Report` no tienen pantalla ni endpoint propio; la pregunta abierta sobre si mantenerlos a largo plazo quedó resuelta por RFC-0027 (se retiran del producto expuesto; el modelo de datos permanece en el schema sin exponerse). El header (`apps/web/src/app/Layout.tsx`, reescrito en RFC-0028 como app-shell — `h-dvh` + `main` con su propio scroll, en vez de un `calc(100dvh-7rem)` hardcodeado que el nav de dos filas en móvil rompía) muestra el logo real de Vectra (`src/assets/vectra-logo.png`, PNG 1024×1024, `size-7 rounded-md`) junto al wordmark de texto. Por debajo de `sm` el nav pasa a una segunda fila propia debajo del header (mismos 4 links, `overflow-x-auto` como red de seguridad); nunca se renderizan las dos filas a la vez.

- **Escenarios** (`/scenarios`): `ScenariosLayout` (master-detail persistente). Desde `md`, sidebar + detalle lado a lado, colapsable (`localStorage` bajo `scenarios_sidebar_collapsed`), reescrito sobre los tokens del sistema (`bg-card`/`border`/`bg-accent`/`text-accent-foreground`/`text-primary`, `shadow-sm` a juego con `Card`). **Por debajo de `md` (RFC-0028)**: la columna se oculta (`hidden md:flex`), el detalle ocupa el ancho completo, y un botón "Escenarios" en una barra de herramientas propia abre la lista dentro de un `Sheet` (drawer desde la izquierda) — elegir un escenario cierra el drawer solo, `Esc` también lo cierra. `ScenarioList` (componente interno) renderiza la lista una sola vez y la reutilizan tanto la columna como el drawer, para que nunca puedan mostrar cosas distintas. Trae los escenarios con `includeArchived: true` (un solo request) y los separa en dos secciones:
  - **Escenarios** (no archivados): orden de dos claves — el escenario con `status: "ACTIVE"` siempre encabeza la lista (aunque no sea el más reciente), y dentro de cada grupo (activo / inactivo) desempata por `updatedAt` descendente. Texto en `text-foreground` (contraste pleno) para distinguirse claramente de Archivados.
  - **Archivados**: solo se muestra si hay al menos uno; texto en `text-muted-foreground/60` (menor énfasis, mismo lenguaje visual — nada de tachado). Clic navega igual a `/scenarios/:id`; "Desarchivar" sigue viviendo en el dropdown del detalle.
  - Ambas secciones renderizan la misma fila (`ScenarioSidebarRow`, componente local no exportado).
  - **Crear escenario navega directo al nuevo** (`ScenarioFormDialog`): tras `createScenario.mutateAsync`, `navigate(/scenarios/:id)` en vez de solo cerrar el diálogo.

  `ScenarioDetailPage`: `ScenarioSummaryCards` (reutiliza `ProjectionStatCards`, el mismo trío mensual/6 meses/anual que Categorías/Productos/Ingresos) + `ScenarioItemsSection` + `ScenarioIncomesSection` + `ScenarioCompositionsSection`, apiladas a ancho completo. `ScenarioItemsSection` es un asistente de 3 estados sin modales (`idle`/`browse`/`create`), montado con `key={scenario.id}`. El título de la tarjeta es simplemente **"Productos"** en los tres estados (RFC-0026: se revirtió el subtítulo "Productos · {categoría}" del header); la categoría elegida se muestra como **`Badge variant="secondary"`** justo encima del checklist/formulario, dentro del área de trabajo — no como texto plano ni como subtítulo del header.
  - `idle`: rejilla de **mini-cards** (`ScenarioItemCard`, `grid-cols-[repeat(auto-fill,minmax(6.5rem,1fr))]`). Sin alto fijo: el nombre reserva dos líneas y el grid estira cada tarjeta al alto de su fila. Jerarquía: nombre (`text-xs font-medium`) → categoría (chip pequeño, menor énfasis) → **monto** (`text-sm font-bold tabular-nums`, lo más destacado) → frecuencia (`text-[10px]` muted). El botón de quitar está oculto hasta hover/foco (siempre visible en touch vía `pointer-coarse:`). El área tiene `min-h-40` para que agregar el primer producto no la haga "colapsar" de alto.
  - `browse` (`ScenarioCategoryChips` + `ScenarioProductChecklist`): fila de chips de categoría con `flex-wrap` (más un chip "+ Nueva") → productos con checkboxes animados. Un producto recién marcado gana un selector de frecuencia inline ("Original/Mensual/Anual/Esporádico") que viaja como `frequency` opcional al agregar.
  - `create` (`ScenarioInlineProductForm`): mismos chips para elegir categoría, luego formulario de nombre/precio/frecuencia. El `useForm` vive en `ScenarioItemsSection`, no en el hijo.
  - **Crear categoría inline** (`ScenarioInlineCategoryForm`): tercer paso posible desde los chips en ambos flujos, sin modal.
  - **"Desactualizado" unificado** (`ScenarioOutdatedIndicator.tsx`): un único borde/fondo ámbar (`scenarioRowClassName`) y un único punto indicador, compartidos por `ScenarioItemCard`, `ScenarioIncomeCard` y `ScenarioCompositionCard`.
  - **`ScenarioIncomesSection`**: ya no es una lista de filas — es un grid de **`ScenarioIncomeCard`** (nueva en RFC-0026, `grid-cols-[repeat(auto-fill,minmax(9rem,1fr))]`), misma familia visual que `ScenarioItemCard` pero más grande/aireada (`p-4`). Sin `text-emerald-600` hardcodeado en el monto — texto neutro `font-semibold tabular-nums`. Mismo `min-h-40` que Productos.
  - **`ScenarioCompositionsSection`**: tampoco es ya una lista de filas — **`ScenarioCompositionCard`** (rediseñada en RFC-0026 de fila a mini-card centrada: nombre, total mensual — gratis de `GET /scenarios`, sin request extra —, cantidad de productos/ingresos, "Desactualizado", botón quitar flotante). Clic abre `ScenarioCompositionDetailDialog` (sin cambios ahí: mensual + grid de `ScenarioItemCard` solo-lectura para productos + lista de ingresos + botón "Abrir escenario"). Mismo `min-h-40`.
  - Las tres secciones (Productos/Ingresos/Escenarios incluidos) comparten ahora contenedor, tratamiento de "Desactualizado", jerarquía tipográfica y el patrón `min-h-40` — se perciben como un solo sistema.

- **Categorías** (`/categories` → `/categories/:id`): responde "¿cuánto gasto en esta área?" + eliminar con reasignación de productos. Lista como `CardGrid density="card"` de **`CategoryCard`** (nueva en RFC-0026, reemplaza `DataTable`): nombre, badge de tipo teñido con el color de la categoría, mensual + cantidad de productos vía `useCategorySummary` propio por tarjeta (N+1 aceptado explícitamente por el usuario — misma escala que `ScenarioCompositionCard`, sin endpoint nuevo), y el fondo completo de la tarjeta teñido suavemente con ese mismo color (`category-color.ts`'s `wash`, `hover:brightness-95`/`dark:hover:brightness-110` — contraste en oscuro confirmado en RFC-0028 con los 8 colores lado a lado, incluidos los pares más cercanos teal/cyan y blue/indigo: se distinguen, sin cambios). `CategoryDetailPage` reutiliza **`ExpenseItemCard`** en modo solo-lectura (`canEdit={false}`) para su lista de productos, ahora como `CardGrid density="tile"` — ya no hay dos representaciones distintas de "cómo se ve un producto" en la app. `CategoryFormDialog` pide solo nombre y tipo. El texto de la página y el diálogo de borrado ya no mencionan transacciones/presupuestos/reportes (RFC-0028; eran residuo textual de RFC-0027) — describen el comportamiento real: bloquea si tiene productos, con el flujo de mover-y-eliminar (`MoveItemsAndDeleteCategoryDialog`) para resolverlo.
- **Productos** (`/expense-items` → `/expense-items/:id`): "¿cuánto me cuesta mantener esto?" + en qué escenarios se usa. Lista como `CardGrid density="tile"` de **`ExpenseItemCard`** (nueva en RFC-0026, reemplaza `DataTable`) — la tarjeta de referencia visual para el resto de la app: nombre, badge de categoría con color derivado, monto en negrita (compacto, RFC-0028), frecuencia, badge "Archivado", menú de 3 acciones (Editar/Archivar/Eliminar) oculto hasta hover, tarjeta completa como link de navegación al detalle. `canEdit` opcional (default `true`) para el modo solo-lectura que reutiliza `CategoryDetailPage`. Confirmado en RFC-0028 (no era un riesgo real): el menú "…" es hermano del `Link`, no está anidado dentro — abrirlo nunca dispara la navegación al detalle.
- **Ingresos** (`/incomes` → `/incomes/:id`): "¿cuánto me genera esta fuente?", o "esporádico, sin proyección" si `ONE_TIME` — **y ahora también "en qué escenarios se usa"** (`ScenarioUsageList`, agregado en RFC-0026). Lista como `CardGrid density="card"` de **`IncomeCard`** (nueva en RFC-0026, reemplaza `DataTable`) — más grande/aireada que `ExpenseItemCard` (pocos ingresos típicamente), monto protagonista solo por tamaño y peso tipográfico (`text-xl font-bold`), **sin color**: decisión explícita para no reabrir el `text-emerald-600` que ya se había retirado de Escenarios por no tener significado semántico.
- **Todas las confirmaciones de borrado usan `AlertDialogAction variant="destructive"`** (RFC-0028) — categoría, producto, ingreso y escenario, desde lista y desde detalle (6 sitios). Antes usaban el botón primario índigo por defecto, la misma apariencia que cualquier acción no destructiva de la app.
- **Desktop, `lg` (1024px) arriba (RFC-0028)**: `PageContainer` ensancha el contenedor de página a `max-w-6xl` (antes `max-w-4xl` en todos los tamaños) y `CardGrid` sube sus mínimos de columna y gaps — Productos pasa de 8 columnas de 104px a 6 de 179px en 1280px de viewport, por ejemplo. Por debajo de `lg` nada cambia frente a lo ya validado. El composer de Escenarios no se tocó (vive dentro del panel de detalle, que sigue en `max-w-4xl`).
- **`ScenarioUsageList`** (`apps/web/src/components/`, nuevo): "en qué escenarios se usa", extraído del bloque que antes vivía inline en `ExpenseItemDetailPage` para no duplicarlo. Vive junto a `ProjectionStatCards` — fuera de ambas features (Productos/Ingresos), ninguna depende de la otra ni de `features/scenarios`. Reutilizado tal cual en `IncomeDetailPage`.

**Toda la UI de la app está traducida al español**, incluyendo mensajes de validación de Zod (`z.config(es())`).

**Sistema de mensajes al usuario (RFC-0029)**: el API **nunca manda español**. Cada error de negocio viaja como un `code` estable (16 valores en `packages/types/src/errors.ts`, importados tanto por `apps/api` como por `apps/web` — no mirrorizados como el resto de schemas, porque una lista divergente rompería el mapeo en silencio) más un `message` técnico en inglés destinado a logs y Swagger. `apps/web/src/lib/error-messages.ts` es **el único sitio del frontend que produce texto de error para el usuario**: `getErrorMessage(error, resource?)` resuelve `code` → copy en español, y cualquier código desconocido (API vieja, gateway, fallo de red) cae al genérico "Algo salió mal. Intenta de nuevo.". `error.message` ya no se renderiza en ninguna pantalla — antes se mostraba crudo en cada toast, que es cómo llegaba a pantalla texto como "Category has associated records". Los códigos genéricos admiten override por recurso (`RESOURCE_IN_USE` dice "este producto…" / "este ingreso…" / "este escenario…"), y `applyConflictError` quedó acotado a `DUPLICATE_NAME` en vez de a cualquier 409: los otros conflictos no son sobre el valor del campo, así que anclarlos bajo él atribuía mal el problema.

**`CATEGORY_HAS_ITEMS` y `CATEGORY_HAS_RECORDS` son códigos distintos a propósito** (RFC-0029). `deleteCategory` contaba cuatro relaciones juntas y devolvía un único mensaje genérico. Los productos se pueden mover a otra categoría (`MoveItemsAndDeleteCategoryDialog` existe para eso); las filas de ledger retiradas en ADR-0007 no tienen ningún flujo que las resuelva, solo archivar. Con un código único, el copy "muévelos a otra categoría" sería mentira en ese segundo caso. El chequeo de ledger va primero porque cuando aplican los dos, mover productos igual deja la categoría sin poder borrarse. Verificado en la sesión: la categoría "Comida" del seed tiene datos residuales de ledger y devuelve `CATEGORY_HAS_RECORDS`, no `CATEGORY_HAS_ITEMS` — exactamente el ítem 16 de esta lista manifestándose.

**El error handler nunca filtra internos, en ningún entorno** (RFC-0029). Antes reenviaba `error.message` crudo salvo en `production`, así que un `PrismaClientKnownRequestError` no capturado exponía nombres de constraint en `development`/`test`. Ahora solo los `HttpError` propios (los que escribimos a mano) reenvían su mensaje; cualquier otra cosa — error de Prisma, `TypeError`, error de framework — responde `INTERNAL_ERROR` con un texto fijo y se loguea completa del lado del servidor. El `notFoundHandler` tampoco devuelve ya el método y la URL solicitados.

**Los formularios usan `noValidate`** (RFC-0029): Zod vía `zodResolver` es la única fuente de validación. Sin esto, la validación nativa del navegador (`type="number"` + `step`, `required`) corre primero y bloquea el submit con un globo cuyo texto sigue el idioma **del navegador**, no el de la app — un monto con 3 decimales mostraba el mensaje nativo en vez de "Usa como máximo 2 decimales", y en un navegador en inglés habría aparecido inglés dentro de una UI en español. Cada campo ya lleva la misma regla en su schema, así que no queda nada sin validar.

**ADR-0005**: sin cambios. **ADR-0006**: `partially superseded` por ADR-0007 en la sección "Ledger histórico: se queda, sin cambios de alcance" — esa sección ya no aplica; el resto (las cuatro preguntas, la jerarquía visual, `/` como aterrizaje en Escenarios) sigue vigente. **ADR-0007** (nueva, `accepted`): retiro del ledger de la experiencia de producto y del backend expuesto (RFC-0027) — ver "Backend"/"Frontend"/"Navegación" arriba. RFC-0023.3 y RFC-0025 se mantienen dentro de lo ya aceptado. RFC-0026 fue mayormente presentación (tarjetas, tokens, layout) en Escenarios/Categorías/Productos/Ingresos, con un único cambio de superficie de API: agregar `scenarios` a `GET /incomes/:id/summary` — simétrico a lo que ya existía para `expense-items`, misma pregunta de negocio (ADR-0006) respondida para el recurso al que le faltaba, no una regla nueva.

## Qué se hizo en esta sesión

**RFC-0029 — auditoría + implementación del copy y los mensajes de la aplicación.** Dos partes: auditoría completa de todos los mensajes visibles (sin código, entregada y aprobada antes de tocar nada) e implementación de la Opción B que el usuario eligió — códigos de error estables en el API, copy controlado por el frontend. Sin cambios de lógica financiera, sin tocar el schema de Prisma, sin features nuevas.

### 1. Auditoría (sin código)

Inventario exhaustivo de mensajes en las 4 features más auth, backend y frontend por separado. El hallazgo que reencuadró el RFC: **no era "traducir unos strings de Zod"**. El frontend era un passthrough del backend — cada toast de mutación mostraba literalmente el `error.message` que mandaba `apps/api`, y `apps/api` no tenía ningún `z.config(es())`: todos sus mensajes estaban en inglés. Es decir, la calidad y el idioma del copy de error dependían enteramente del backend. Segundo hallazgo: el mensaje de "no se puede borrar la categoría" era genérico y no distinguía productos (movibles) de datos de ledger (no movibles), así que la única salida que el copy podía ofrecer honestamente no coincidía con el flujo real del frontend.

### 2. Contrato de errores (Opción B, elegida por el usuario)

16 códigos en `packages/types/src/errors.ts`; `HttpError` del API los lleva; ~35 `throw` etiquetados en los 5 servicios más `plugins/auth.ts` y `auth.routes.ts`. Detalle del diseño y de por qué `CATEGORY_HAS_ITEMS` y `CATEGORY_HAS_RECORDS` son códigos separados: ver "Estado" arriba.

### 3. Capa de copy del frontend

Nuevo `apps/web/src/lib/error-messages.ts` (único productor de texto de error), `ApiError` gana `code`, `applyConflictError` acotado a `DUPLICATE_NAME`. 16 componentes de features migrados. Verificado por grep: cero ocurrencias de `error.message` renderizado y cero `toast.error` fuera de la capa.

### 4. Copy de validación y toasts de éxito

Mensajes en español en los schemas de entrada de `packages/types` (nombre, categoría, monto, email, contraseña, frecuencia, tipo). Los schemas de respuesta se dejaron sin mensajes a propósito: validan payloads del servidor, que ningún usuario ve. Toasts de éxito para crear/editar/archivar/restaurar/eliminar en las 4 entidades más activar/desactivar escenario. **No** se agregaron toasts en el composer de escenarios (agregar/quitar producto, vincular ingreso, componer): el grid muta a la vista justo donde el usuario hizo clic, y un toast por checkbox sería ruido — ese es el corte de "redundante" que pidió el usuario.

### 5. Ajustes finales pedidos tras la primera verificación

- **`noValidate` en los 5 formularios** — detalle en "Estado" arriba. Se descubrió midiendo: el check de "más de 2 decimales" salía vacío en el navegador porque la validación nativa bloqueaba el submit antes que Zod.
- **Copy residual del ledger**: `CategoryFormDialog` todavía decía "Las categorías agrupan tus transacciones para presupuestos y reportes" — features retiradas en RFC-0027. Ahora dice "Las categorías agrupan tus productos por área de tu vida". RFC-0028 había corregido este residuo en la página de Categorías pero no en este diálogo.

### 6. QA

`typecheck`/`lint`/`build`/tests (59 API + 1 web) en verde; los tests no asertaban sobre mensajes, así que ninguno se rompió al reescribirlos. Verificación en dos capas, ambas desde el scratchpad:

- **API directo (21 casos)**: cada código de negocio verificado contra su endpoint, con fixtures limpias creadas y borradas para alcanzar los que el seed no permite (`CATEGORY_HAS_ITEMS` sin ledger, ingreso vinculado a escenario). 0 fugas de Prisma/SQL/constraint/stack en las respuestas.
- **Navegador (Edge vía `playwright-core`)**: formularios vacíos de producto/categoría/ingreso, nombre duplicado, monto inválido, eliminar producto en uso, toast de éxito. Escaneo automático de todos los textos capturados contra patrones de inglés y de detalle técnico: ninguna coincidencia.

Dos expectativas de la verificación resultaron estar mal en el script, no en el código, y quedan anotadas para no re-investigarlas: renombrar un escenario archivado **sí** está permitido (`updateScenario` no llama a `assertNotArchived` a propósito; solo activar/componer/agregar lo hacen), y un `DELETE` con `Content-Type: application/json` sin body lo rechaza Fastify antes de llegar a la ruta (por eso `api-client.ts` solo manda ese header cuando hay body).

## Sigue — próximos pasos y mejoras

### Pendiente inmediato

1. **Dos observaciones de pulido visual detectadas al cerrar RFC-0028, no corregidas a propósito** (fuera del alcance pedido, "ajuste puntual" no "rediseño"):
   - El tinte de la categoría _Entretenimiento_ (fuchsia/pink en `category-color.ts`) lee bastante rojizo en modo oscuro, cerca del significado destructivo que la paleta quiso evitar a propósito (ver "Estado" — por eso excluye ámbar y rojo). No es uno de los 8 colores con el problema del contraste (ese ítem sí se confirmó resuelto), es un problema de significado.
   - El botón "Mostrar archivadas/os" de Categorías/Ingresos queda solo en su fila a la derecha; en `lg` con el contenedor más ancho se ve algo huérfano, separado del header. Mover el toggle al header lo resolvería, pero se dejó así para no arriesgar el layout de 320px que RFC-0028 dio por cerrado.

### Funcionalidad pendiente (analizada, no construida)

2. **Métricas "¿cuánto dinero me queda?" a 6 y 12 meses** — hoy `incomeCoverage.remainingMonthly` solo existe a nivel mensual.
3. **Propios vs. heredados en la lista de productos de un escenario compuesto** — `ScenarioItemsSection` (la pantalla del propio escenario) sigue mostrando solo los items propios (`listScenarioItems` no recorre la composición), aunque el total y el sync sí la recorren.
4. **Comparador de escenarios** con deltas contra el escenario activo (ADR-0005 §12) — introduce Recharts. **Candidato más alto en valor para la próxima sesión**, y con el golden path de RFC-0023.3 ya confirmado (ver arriba) desapareció la razón para posponerlo: era la validación pendiente sobre la que este comparador se apoyaría. Explícitamente fuera de alcance en RFC-0028 y RFC-0029, a pedido del usuario.
5. **Editar la frecuencia de un producto ya incluido en un escenario** (no solo al agregarlo).

> Los ítems "validación visual manual en navegador" y "otra pasada para unificar visualmente los detalles" quedaron **resueltos** por RFC-0028: 56+21 checks de overflow y 20 de flujos verificados en navegador real (Edge/`playwright-core`), y `DetailPageHeader`/`PageContainer` unificaron el header y el contenedor de las tres páginas de detalle. El **golden path de RFC-0023.3**, que figuraba aquí como el pendiente más antiguo, quedó **resuelto** en la sesión del 2026-08-12 (ver la cabecera de este documento): se validó manualmente y no requirió cambiar ningún archivo, por eso no hay commit asociado.

> Nota: el ítem "rediseño del Dashboard alrededor de escenarios" que figuraba aquí quedó resuelto (no pendiente) por RFC-0027 — Dashboard se retiró del producto en vez de rediseñarse; ver ADR-0007.

### Deuda técnica / mejoras menores

6. **`framer-motion@11.15` es anterior a React 19** — causó el bug del "primer clic" de varias sesiones atrás, ya corregido. No urge migrar a `motion` v12 salvo que reaparezca algo raro con animaciones. Sin relación con el bug de animación de diálogos de RFC-0028 (ese era CSS/Tailwind puro, nada de framer-motion de por medio).
7. **Playwright sigue sin estar en el repo** — van ya varias sesiones (RFC-0028 y RFC-0029 incluidas) usándolo ad hoc desde el scratchpad (`playwright-core` + canal `msedge`, sin descarga de navegadores). Vale la pena evaluarlo como `devDependency` real. Nota operativa de RFC-0029: el CORS del API solo permite `http://localhost:5173`, así que si Vite arranca en 5174 porque 5173 está ocupado, el login falla por CORS y cualquier verificación en navegador se cae — apuntar siempre a 5173.
8. **`prisma migrate` no funciona en esta máquina** (`spawn UNKNOWN` del schema-engine). Toda migración futura necesita el rodeo manual (SQL directo + registro en `_prisma_migrations`), o arreglar el entorno.
9. **`getScenarioSummary` llama a `detectScenarioChanges` internamente** para derivar `hasUpdates` — suma consultas por cada carga de summary. Aceptable a esta escala.
10. **`GET /scenarios` con N consultas internas por fila** (una por escenario, para el `monthly` enriquecido) — el sidebar y `ScenarioCompositionCard` dependen de este campo.
11. **N+1 en el cliente, mismo criterio en varios lugares**: `ScenarioCompositionCard` (conteo de productos/ingresos por escenario heredado) y `CategoryCard` (mensual + conteo de productos por categoría vía `useCategorySummary`). Aceptable con la cantidad típica de filas por página (1-3 composiciones, decenas de categorías), mismo criterio que el resto del proyecto.
12. **Suite de tests en `apps/api`**: `apps/api/tests/{categories,expense-items,incomes,scenarios}.test.ts` — 4 archivos, ~59 casos (`it(...)`) en total. Cubren el dominio de escenarios vigente; no hay tests para `auth` ni para los endpoints de sincronización granular (`/sync-scenarios`, `/scenarios/:id/sync`). Ni RFC-0028 ni RFC-0029 agregaron tests automatizados de frontend (el único `apps/web/tests` sigue siendo el de `CategoriesPage`); ambas se verificaron manualmente en navegador. **Los tests del API no asertan sobre mensajes de error, solo sobre status codes** — por eso RFC-0029 pudo reescribir todos los mensajes sin romper ninguno, pero también significa que **ningún test cubre los `code` nuevos**: hoy su única red es la verificación manual de esa sesión. Candidato claro para la próxima tanda de tests.
13. **Los endpoints `/scenarios/:id/changes(/apply)` no tienen consumidor** — conservados como capacidad futura. Van varias sesiones sin uso; evaluar eliminarlos si sigue así.
14. **`packages/ui`'s `DataTable` quedó sin ningún consumidor en `apps/web` tras RFC-0027** — sus tres usos eran Cuentas/Transacciones/Dashboard, los tres retirados. No se eliminó en RFC-0027 a propósito (ADR-0007); candidato a limpieza cuando se decida qué hacer con `packages/ui`.
15. **Las tablas `accounts`, `transactions`, `budgets`, `recurring_transactions` quedan en Postgres sin ningún endpoint que las lea o escriba** — dato residual aceptado a propósito por ADR-0007. Una futura limpieza de schema (dropear estos modelos de Prisma) es un RFC aparte, no decidido todavía.
16. **`recharts` está en `apps/web/package.json` sin ningún consumidor** desde que RFC-0027 retiró el Dashboard (sus únicos usos eran ahí). Sigue instalado porque el comparador de escenarios (ítem 4 arriba) lo necesitaría si se construye — no se desinstaló a propósito, no por descuido.

### Decisiones de producto sin resolver (a propósito, no ahora)

17. Si los ingresos deberían agruparse por categoría en algún momento.

> El ítem "si `Budget`/`Reports` se mantienen a largo plazo" que figuraba aquí quedó **resuelto** por RFC-0027: se retiran del producto expuesto (ver ADR-0007).

**Usuario de prueba con datos**: `dev@vectra.local` / `devpassword`. El seed hoy solo crea usuario + categorías por defecto (`createInitialUserData` dejó de sembrar una cuenta "Efectivo" en RFC-0027); cualquier account/transaction/budget sembrado en sesiones anteriores puede seguir residiendo en la base como dato residual sin endpoint que lo exponga (ver ítem 16). El escenario "Repro primer clic" (20 productos en Comida) sigue ahí — se reutilizó en RFC-0028 como caso de prueba real para la densidad de grids en desktop; sigue siendo útil, no hace falta borrarlo. RFC-0028 además sembró una categoría de nombre largo, un producto de 7 cifras y un ingreso de 8 cifras en COP/USD para forzar los casos que la auditoría señaló — pueden quedar como datos de prueba permanentes.
