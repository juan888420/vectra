{
  "name": "@vectra/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo \"apps/api: nothing to run yet, scaffolded in RFC-0004\"",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.0",
    "@prisma/client": "^7.9.0",
    "@vectra/types": "workspace:*",
    "@vectra/utils": "workspace:*"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "prisma": "^7.9.0",
    "tsx": "^4.23.1",
    "typescript": "^5.7.0"
  }
}
