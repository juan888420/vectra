# Separate database for integration tests, never the dev database.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vectra_test?schema=public"
JWT_SECRET="test-only-secret-do-not-use-in-any-real-environment"
NODE_ENV="test"
